# SPDX-License-Identifier: GPL-3.0-or-later
name: Normalize SIM provide-PIN evidence

on:
  push:
    paths:
      - '.github/workflows/normalize-sim-provide-pin-evidence.yml'

permissions:
  contents: write

jobs:
  normalize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python tools/normalize_sim_provide_pin_evidence_once.py
      - run: python tools/validate_public_repo.py
      - name: Commit normalized live SIM provide-PIN evidence
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add specification/methods.json api/sim.md CHANGELOG.md
          git diff --cached --quiet && exit 0
          git commit -m 'Record live SIM provide-PIN evidence'
          git push
